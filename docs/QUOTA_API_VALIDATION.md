# 自动额度读取验证

日期：2026-09-08，macOS / Apple Silicon，Typeless 官方客户端 2.5.0。

## 结果

- 原生 Swift 查询通过：已用 652 / 8000，剩余 7348；服务端身份匹配，请求前后会话一致。
- 实际安装到 `/Applications/Typeless++.app`，Developer ID 签名验证通过。
- 实际点击“自动读取官方额度”：初始关闭，开启后显示额度，重启/账户页/主页核对提示消失。
  未操作官方客户端页面，也未改变官方登录账号。再次点击“刷新”后同步时间更新。
- 本机已保留启用状态，2 个账号条目不变，低额度守护仍关闭。
- 自动额度不改变录音/处理状态判断；未知活动仍保持 unknown。

## 自动化证据

| 检查 | 结果 |
|---|---|
| 直接查询的初始失败回归 | 6 项测试、14 个断言失败，记录旧实现的缺口 |
| API/会话、控制器、目录集成和文件事件聚焦检查 | 35 项通过 |
| 原生真实额度请求 + URL/重定向边界专项 | 2 项通过 |
| `make verify` | 193 项 Swift 测试，188 通过、5 项 opt-in 实机测试默认跳过；8 项 Python 测试通过；应用包/签名/迁移脚本/更新 feed 检查通过 |
| Debug / Release warnings-as-errors | 均通过 |
| 变更文档敏感扫描 | 通过；源码扫描命中的 4 项均是主分支已有测试赋值，无新凭证 |

五项默认跳过包括四项既有客户端读取测试，以及本次新加入的真实额度请求测试；本次额度请求已通过
显式设置 `TYPELESS_PLUSPLUS_RUN_LIVE_OFFICIAL_QUOTA_QA=true` 单独执行，不以默认跳过冒充实测。

测试涵盖身份不符、请求期间换号、同邮箱新会话、迟到结果、401/429/无效响应、重定向拒绝、
两个平台的会话加密格式、并发事件合并、手动冷却、Retry-After 秒数、缓存过期、关闭功能及
SQLite WAL 原地写入/会话文件原子替换。完成回调不产生持续网络轮询。

## 回退及范围

新额度来源在账户目录中记为 `typelessOfficialAPI`。旧构建不识别这一来源，因此回退应用时需同时
恢复升级前的 `accounts.json`；本轮已备份旧应用压缩包和该文件，并保留本机恢复说明。备份不复制
官方 Typeless 会话、Keychain 或设备身份。公开的可移植备份仍不含额度，格式不变。

本轮没有重新执行真实双账号往返；账号代次竞态由测试覆盖。Intel 仅完成离线格式测试，真实 Intel
运行、服务器下发瞬时提示和真实录音/处理控件的现场验证均为 UNVERIFIED。官方内部接口未来可能
变化，失败时显示不可读并保持数据边界。此结果不代表公开发布或更新 feed 已更新。
